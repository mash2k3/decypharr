name: Usenet pre-cache on open config test image

on:
  push:
    branches: [fix/usenet-precache-on-open-config]
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/zab1996/cli_mount:usenet-precache-on-open-config
          build-args: |
            VERSION=2.4.9
            CHANNEL=usenet-precache-on-open-config
